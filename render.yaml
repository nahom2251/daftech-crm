services:
  # ---------- Angular frontend (static site) ----------
  - type: web
    name: daftech-crm
    runtime: static
    rootDir: ng/daftech-crm
    buildCommand: npm ci && npm run build
    staticPublishPath: dist/daftech-crm
    routes:
      - type: rewrite
        source: /*
        destination: /index.html